{
  "name": "record-log-lambda",
  "version": "1.0.0",
  "description": "Lambda function for recording logs on blockchain via Fireblocks",
  "main": "tx-sender/index.js",
  "scripts": {
    "test": "mocha test/**/*.test.js"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.499.0",
    "@aws-sdk/client-s3": "^3.876.0",
    "@aws-sdk/client-dynamodb": "^3.876.0",
    "@aws-sdk/lib-dynamodb": "^3.876.0",
    "@fireblocks/fireblocks-web3-provider": "^1.2.2",
    "ethers": "^6.7.1"
  },
  "devDependencies": {
    "chai": "^5.2.0",
    "fs-extra": "^11.3.0",
    "mocha": "^11.1.0",
    "proxyquire": "^2.1.3",
    "sinon": "^20.0.0"
  }
}